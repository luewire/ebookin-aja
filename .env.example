# ============================================
# ENVIRONMENT VARIABLES TEMPLATE
# ============================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git!
#
# For Vercel deployment, set these in:
# Project Settings → Environment Variables
# ============================================

# ============================================
# PUBLIC VARIABLES (Safe for client-side)
# ============================================
# These are prefixed with NEXT_PUBLIC_ and will be exposed to the browser
# Only put non-sensitive values here!

# Supabase Project URL (public, safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon/Public Key (public, safe to expose - has RLS protection)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxxxxxxxxxxxxxxxx...

# Optional: Supabase Publishable Key (if using realtime features)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_xxxxx

# ============================================
# PRIVATE VARIABLES (Server-side ONLY)
# ============================================
# These should NEVER have NEXT_PUBLIC_ prefix
# Only accessible in API routes and server components
# CRITICAL: Keep these secret!

# Supabase Service Role Key (ADMIN ACCESS - bypasses RLS!)
# ⚠️  NEVER expose this to client-side code
# ⚠️  NEVER commit this to git
# ⚠️  Only use in /app/api/* routes
SUPABASE_SERVICE_ROLE_KEY=eyJxxxxxxxxxxxxxxxxx...

# ============================================
# DEPLOYMENT NOTES
# ============================================
# 
# LOCAL DEVELOPMENT:
# 1. Copy this file to .env.local
# 2. Fill in your Supabase credentials from https://supabase.com/dashboard
# 3. Never commit .env.local to git
#
# VERCEL DEPLOYMENT:
# 1. Go to Project Settings → Environment Variables
# 2. Add each variable with its actual value
# 3. Set for Production, Preview, and Development as needed
# 4. Never store secrets in git, even in Vercel config files
#
# SECURITY CHECKLIST:
# ✅ .env.local is in .gitignore
# ✅ SERVICE_ROLE_KEY only used in /app/api/* routes
# ✅ NEXT_PUBLIC_* prefix only on safe, public values
# ✅ No hardcoded secrets in code
# ✅ Secrets stored in Vercel Environment Variables
# ============================================
